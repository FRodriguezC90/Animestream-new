// Coloca tu código JavaScript personalizado aquí
// Por ejemplo, para validar el formulario de inicio de sesión

document.addEventListener('DOMContentLoaded', function () {
    const loginForm = document.getElementById('loginForm');

    loginForm.addEventListener('submit', function (e) {
        e.preventDefault();

        // Realiza la validación del formulario aquí
        // Puedes usar JavaScript para enviar los datos del formulario al servidor
    });
});
